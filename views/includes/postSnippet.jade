div.post
  span.date #{post.date.toISOString().slice(0, 10)}
  |  
  a(href=post.url)
    = post.title
//  div.post.content
//    != post.preview
//  div.tags
//    - each tag in post.tags
//      a(href=tagUrl(tag))
//        span.label.label-info #{tag}
